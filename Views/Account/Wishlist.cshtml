@{
    ViewData["Title"] = "Wishlist";
}

<div class="container mt-4">
    <h2>Your Wishlist</h2>
    <ul>
        @foreach (var item in Model)
        {
                <li>@item</li>
        }
    </ul>
</div>
